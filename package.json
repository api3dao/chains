{
  "name": "@api3/chains",
  "license": "MIT",
  "version": "1.4.0",
  "private": false,
  "main": "src/index",
  "files": [
    "chains",
    "src"
  ],
  "scripts": {
    "build": "yarn build:chains && tsc",
    "build:chains": "ts-node scripts/combine-chains.ts",
    "dev": "ts-node scripts/combine-chains.ts --watch",
    "chains:rename": "ts-node scripts/rename-chain-file.tss",
    "env:write": "ts-node scripts/write-env-variables.ts",
    "prettier": "prettier --write \"./**/*.{js,md,json}\"",
    "validate:chains": "ts-node scripts/validate-chains.ts",
    "validate:providers": "ts-node scripts/check-provider.ts"
  },
  "dependencies": {
    "zod": "^3.21.4"
  },
  "devDependencies": {
    "@types/node": "^18.15.11",
    "chokidar": "^3.5.3",
    "ethers": "^6.0.8",
    "prettier": "^2.8.4",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.3"
  }
}
